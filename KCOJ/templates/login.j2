<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <link rel="stylesheet" type="text/css" href="/static/css/global.css">
    <link rel="stylesheet" type="text/css" href="/static/css/login.css">

    <title>{{ title }}</title>
</head>
<body>
    <section>
        <div class='caption-pad'>
            <h1>Kuo Cool Online Judge</h1>
            <h2 class='color-deep-gray'>重新設計操作流程，打造美好交作業體驗。</h2>
        </div>
        <div class='login-pad'>
            <h2 class='color-theme'>請輸入登入資訊</h2>
            <!-- 登入表單 -->
            <form action="/login" method="POST">
                <div class="input-group mb-3 mt-5">
                        <input type="text" name="userid" class="form-control" placeholder="學號" aria-label="Username" aria-describedby="basic-addon1">
                </div>
                <div class="input-group mb-3">
                        <input type="password" name="passwd" class="form-control" placeholder="密碼" aria-label="Username" aria-describedby="basic-addon2">
                </div>
                <div class="input-group mb-3">
                    <select class="custom-select" name="course" id="inputGroupSelect01">
                        {% for name in courses %}
                            <option value={{ name }}>{{ name }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="input-group d-flex justify-content-center">
                        <button type="submit" class="btn btn-primary">登入</button>
                </div>
            </form>
            <p class='mt-note text-center color-deep-gray'>&copy; Kuo Cool Online Judge 2018-2019</p>
        </div>
    </section>

    <script>
        {% if error_message %}		
            alert("{{ error_message }}")
        {% endif %}
    </script>
</body>
</html>